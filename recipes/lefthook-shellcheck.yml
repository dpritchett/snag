pre-commit:
  jobs:
    - name: shellcheck
      glob: "*.sh"
      run: shellcheck {staged_files}
      fail_text: >
        shellcheck found issues in staged shell scripts.
        https://www.shellcheck.net
